---
spec: specs/completed/typescript-migration.md
status: complete
branch: implement/typescript-migration
pr: https://github.com/Ianfeather/big-shop/pull/58
---

## Session 1: Tooling, config, and codegen
Status: done
Scope: typescript + @types/* + openapi-typescript devDeps; tsconfig.json; delete jsconfig.json; .gitignore next-env.d.ts; npm scripts (typecheck, generate:api-types, package); generate+commit types/api.d.ts; extend build.sh drift check; types/swagger-ui-react.d.ts shim. No source conversions.
Depends on: none
Commit: 3cc29b2 (fixes on top of 90449e8)
Notes: Test gate green (tsc --noEmit clean, npm run test 94/94, npm run build clean). Note: @types/react/@types/react-dom had to be pinned to ^18 (npm resolved ^19 by default, mismatching the installed react@18.2.0); typescript pinned to ^5 (openapi-typescript@7.13.0 peer-depends on typescript ^5.x, conflicts with the ^7 npm resolved by default). Review gate: Standards + Spec sub-agents both flagged the same CONFIRMED issue (tsconfig `include` used the unscoped create-next-app default instead of being scoped to pages/components/hooks/types per spec) plus a stale CLAUDE.md doc and a minor build.sh duplication smell — all three fixed in 3cc29b2. `go` isn't installed locally so the Go/drift portions of build.sh couldn't be run end-to-end here (matches CLAUDE.md's documented local limitation); the new types/api.d.ts drift-check line was verified in isolation instead.

## Session 2: hooks/
Status: done
Scope: convert 8 hook files + 6 test files to .ts; type use-recipes.ts/use-recipe.ts against generated RecipeSummary/Recipe.
Depends on: Session 1
Commit: 59b6eef (fix on top of 5a0dd41)
Notes: Test gate green (tsc --noEmit clean, npm run test 94/94, lint clean, build clean). Supporting changes beyond the 8 hooks, needed to make this compile: added types/models.ts (hand-written named aliases over generated types/api.d.ts — spec review flagged this as not literally named in the spec, which only says "type against RecipeSummary/Recipe from types/api.d.ts"; kept deliberately as a low-risk, low-noise convenience layer rather than repeating components['schemas']['X'] indexing at every call site — judgment call, not reverted); converted mocks/index.js -> .ts (code wrapper, not data — required for resolveJsonModule inference to reach through it since allowJs is false); added types/testing-library-jest-dom.d.ts (ambient reference for jest-dom's Vitest matcher types, since vitest.setup.js is plain .js and outside tsconfig's include); vitest.config.js gained esbuild.jsx:'automatic' (native .tsx handling was falling back to esbuild's classic transform, needing React in scope — use-overflow.test.tsx is the first .tsx file in the repo). use-http's cachePolicy is a real enum (CachePolicies.NO_CACHE), not a string literal union. use-ingredient-metadata.ts split one useFetch() into two typed instances (use-http's TData is fixed per instance); review flagged this as an untested but correct behavior improvement (previously response.ok reflected whichever of two concurrent requests resolved last) — not covered by a new test since no hook test in this repo exercises the real-fetch (non-mock) branch, consistent with existing convention. Review gate: Standards sub-agent flagged use-auth.ts's original `as typeof useMockAuth0` cast as an unchecked assertion — fixed in 59b6eef by declaring an explicit UseAuthResult interface and typing the ternary against it, so both branches are checked assignments instead of a blind cast.

## Session 3: components/
Status: done
Scope: convert all 16 component directories to .tsx (+ .test.tsx); explicit prop interfaces.
Depends on: Session 2
Commit: 971db47 (fix on top of 19b13ac)
Notes: Test gate green (tsc --noEmit clean, npm run test 94/94, lint clean, build clean). types/models.ts extended with Invite, ListIngredient, Ingredient. hooks/use-auth.ts (Session 2) needed a follow-up fix: identity/create and identity/logout call loginWithRedirect/logout with option objects the Session 2 zero-arg UseAuthResult didn't support - widened to RedirectLoginOptions/LogoutOptions (real Auth0 types). Deliberate typing exceptions (documented in 19b13ac's commit message): Button.tsx rest-props loosely typed (dual Link/button render target); Tabs.tsx's children convention (ReactElement<{name}>[], not a real HTML attribute); Form.tsx's single useFetch() instance left untyped (backs 5 different-shaped endpoints) with results annotated per call site; sortByDepartment (follow-ups.md item 10's known-incomplete comparator) got an explicit `return 0` fallthrough, verified behavior-identical per ECMA-262's sort-comparator coercion. Review gate: Standards + Spec sub-agents both independently confirmed the exceptions were implemented as documented and found no scope creep; Standards flagged 3 undocumented (recipeTags ?? [])/(recipe.tags || []) null-guards (RecipeSummary.tags/Recipe.tags are nullable in the schema) lacking the explanatory comment Form.tsx's equivalent fix got - fixed in 971db47.

## Session 4: pages/ + pages/api/*
Status: done
Scope: convert remaining pages/*.js and pages/api/*.js/.mjs to .ts/.tsx; remove dead jsx-in-js vitest plugin; confirm allowJs false.
Depends on: Session 3
Commit: 17b3448 (f90f05c + a follow-ups.md doc commit)
Notes: Test gate green (tsc --noEmit clean, npm run test 94/94, lint clean, build clean). All 11 pages + 6 pages/api/* files (+ tests) converted; vitest.config.js's dead jsx-in-js plugin removed (no .js/.jsx remains under pages/components/hooks); tsconfig.json's include/allowJs already correct from Session 1, reconfirmed. Two real, pre-existing bugs surfaced by strict typing and fixed (documented in follow-ups.md items 9 and 12, since not expressible as type-only changes): (1) pages/api/recipe-image.ts imported a nonexistent `Blobs` class from @netlify/blobs@8.1.2 (only Store/getStore/getDeployStore are actually exported) - every call would have thrown TypeError, so Photo Import's job-status persistence was completely non-functional; switched to getStore(). (2) pages/recipes/[id]/index.tsx and edit.tsx both passed a dead `recipes` prop to RecipeList (confirmed via its actual prop interface that it's never read - RecipeList always fetches its own via useRecipes()) - removed the dead prop and unused hook call. Supporting sidecar type declarations added for modules outside the spec's scope that pages/api/* imports: lib/recipe-import/*.d.ts (4 files, .js implementations untouched) and types/formidable.d.ts - same mechanism as Session 1's swagger-ui-react shim, applied to untyped internal/third-party modules. Cross-session fixes to earlier Sessions' files, all required by real Session 4 callers: components/layout/index.tsx (Grid/Sidebar/MainContent didn't accept the `name` prop several pages pass; extracted shared SemanticContainerProps), components/recipe-list/index.tsx + shopping-list/Recipes/index.tsx (selectedIds/recipeList widened Record<number,boolean> -> Record<string,boolean>, since pages/list.tsx indexes with e.target.id, a string - TS allows numeric indexing into a string-keyed record but not the reverse), components/dave-chat/index.tsx (exported its local DaveMessage interface for reuse). Review gate: Standards + Spec sub-agents both found zero CONFIRMED violations and no scope creep - every out-of-Phase-4 file change traced to a real compile requirement or a disclosed bug fix. Standards noted one additional undisclosed-but-benign shape change (pages/list.tsx's addExtraItem placeholder object gained explicit isBought/department/recipe_id fields to satisfy ListIngredient) and confirmed it's behavior-preserving (already commented, matches the placeholder-values convention documented for Session 3's shopping-list work) - no fix needed.

**Migration complete.** All 4 Sessions done. Next: gather evidence and open the PR (see specs/typescript-migration.md and ADR-0002 for full context).
